@model IEnumerable<SchoolManagementSystem.Data.Entities.Department>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Department1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HeadOfDepartment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DepartmentStartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfStudent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Department1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeadOfDepartment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DepartmentStartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfStudent)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartmentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartmentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartmentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
